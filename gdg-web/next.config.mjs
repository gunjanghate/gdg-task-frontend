/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tse3.mm.bing.net', 'drive.google.com'], // Add all domains in one array
    },
};

export default nextConfig;
